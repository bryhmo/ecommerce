{
  "name": "ecommerce-fullstack",
  "version": "1.0.0",
  "description": "A complete ecommerce website built with Next.js and NestJS",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspace @ecommerce/api dev & yarn workspace @ecommerce/web dev",
    "build": "yarn workspace @ecommerce/api build && yarn workspace @ecommerce/web build",
    "start": "yarn workspace @ecommerce/api start",
    "test": "yarn workspaces run test",
    "lint": "yarn workspaces run lint"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
